# # src/main/resources/application.properties

# # Database connection URL
# spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB:tech_stacks_todos}

# # Database credentials
# spring.datasource.username=${MYSQL_USER:root}
# spring.datasource.password=${MYSQL_PASSWORD:MyPass}

# # Hibernate properties
# spring.jpa.hibernate.ddl-auto=update  # or create-drop, none, validate depending on your needs
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# # Connection pool configuration (optional but recommended)
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.hikari.maximum-pool-size=10


# src/main/resources/application.properties

spring.datasource.url=jdbc:mysql://localhost:3306/tech_stacks_todos
spring.datasource.username=root
spring.datasource.password=MyPass
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
